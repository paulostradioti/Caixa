@using Caixa.Web.Data 
@model IEnumerable<Aluno>

@{
    ViewData["Title"] = "Alunos";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1 class="mb-4">Alunos</h1>

<p>
    <a asp-action="Create" class="btn btn-outline-primary mb-3">Cadastrar Novo Aluno</a>
</p>

<div class="table-responsive">
    <table class="table table-hover align-middle">
        <thead class="table">
            <tr>
                <th>@Html.DisplayNameFor(model => model.Id)</th>
                <th>@Html.DisplayNameFor(model => model.Name)</th>
                <th>@Html.DisplayNameFor(model => model.Turma)</th>
                <th class="text-end">Ações</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var aluno in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(_ => aluno.Id)</td>
                    <td>@Html.DisplayFor(_ => aluno.Name)</td>
                    <td>@Html.DisplayFor(_ => aluno.Turma.Nome)</td>
                    <td class="text-end">
                        <a asp-action="Edit" asp-route-id="@aluno.Id" class="btn btn-sm btn-secondary me-1">Editar</a>
                        <a asp-action="Delete" asp-route-id="@aluno.Id" class="btn btn-sm btn-danger">Excluir</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
